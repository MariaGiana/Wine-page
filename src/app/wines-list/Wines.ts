export interface Wines {
    wineName: string;
      grapeVariety: string;
      winery: string;
      description: string;
      price: number;
      image: string;
      stock: number;
      clearance: boolean;
      quantity: number;
}
